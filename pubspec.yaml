name: achivo
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  # Icons for iOS style
  cupertino_icons: ^1.0.8

  # SQLite support
  sqflite: ^2.3.3+1
  path: ^1.9.0
  path_provider: ^2.1.5

  # State management (optional but useful later)
  provider: ^6.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  sqflite: ^2.3.0


flutter:
  uses-material-design: true

  # Assets (add your images/fonts here later if needed)
  # assets:
  #   - assets/images/
  #   - assets/fonts/
